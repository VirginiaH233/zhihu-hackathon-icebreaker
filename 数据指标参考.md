# 数据指标参考

> 这份文档回答三件事：**每个指标是什么、为什么这么定、看到数字该做什么决策。**
> 看板地址：`/dashboard`（线上，随时打开都是最新）｜原始数据：`/api/stats`

---

## 一、北极星指标（North Star Metric）

**定义**：`copy_comment` —— 用户**复制走**的评论条数。

**为什么是它，而不是"访问量"或"生成数"**：

| 候选 | 为什么不用 |
|---|---|
| 访问人数 | 「来过」和「有用」差十万八千里。人来看了，不代表产品成立 |
| 生成评论数 | 生成了**没发出去**也算 → 高估价值 |
| **复制评论数** ✅ | 复制 = 用户**打算真的发出去**。产品的承诺是「帮你开口」，所以「成功」必须定义在"真的要用"这一步 |

**记住这条判据**（以后做任何产品都适用）：

> **北极星必须贴着产品承诺。「承诺什么，就用什么当成功。」**
> 承诺"帮你开口" → 那就得测到"开口"为止，不能停在"帮你准备好素材"。

**看到它该做什么**：它不涨，先看漏斗哪一步掉（下面第二节）。

---

## 二、漏斗（Funnel）

流量路径，也是用户真实的旅程：

```
访问 → 搜人 → 读 TA → 建自己分身 → 拿到评论 → 复制走 ★
visit   search   soul        me          comment    copy_comment
```

**为什么需要漏斗**：没有它，用户反馈只能是「不好用」（无法行动）；有了它，就变成「**60% 的人在搜人之后就走了**」（可行动）。

**怎么用**（看板里每一步右边有个转化率）：

| 哪一步掉得狠 | 通常意味着 | 该动哪里 |
|---|---|---|
| 访问 → 搜人 | 首屏没勾住 / 不知道该输什么 | 首屏文案、示例人选 |
| 搜人 → 读 TA | 搜不到人，或生成太慢 | 看「搜不到的人」那一栏 |
| 读 TA → 建分身 | 不愿授权登录（隐私顾虑） | 登录门的说明怎么写 |
| 建分身 → 拿评论 | 聊得没意思 / 评论质量差 | 回到 prompt 层 |
| **拿到评论 → 复制走** | **生成了但不想用** ← 最致命 | 评论是否真的"能发得出去" |

**最后那个落差最要命**：它意味着「我们生产了用户不想用的东西」。这也是为什么北极星选 `copy_comment` 而不是 `comment`。

---

## 三、定性信号（数字背后的"为什么"）

漏斗告诉你「哪里漏」，这三个告诉你「为什么漏」。

| 信号 | 回答什么问题 | 典型行动 |
|---|---|---|
| **搜不到的人** | 用户搜了谁但没搜到 | 反复出现的名字 → 搜人路径有洞，去修 |
| **热门答主** | 谁被聊得最多 | 给他预热档案（被点开时秒出）；放他进首屏示例 |
| **最近报错** | 线上有没有炸 | 空了就是好消息；有就立刻看是哪条路径 |

---

## 四、信效度（诚实说：这套数据的局限）

**效度 = 这个指标真的代表了我想要的价值吗？**
**信度 = 同样的条件再测一次，结果一致吗？**

我们用过的检查题：**"如果这个数字涨了，我能确定产品真的更好吗？"** ——答不上来，就是效度有问题。

### 已经修的

| 问题 | 修法 |
|---|---|
| 北极星曾用 `comment`（测"生成"不是"发出"）| 改用 `copy_comment` |
| 访问没带 uid → 只能算 PV，算不出 UV | 事件带 uid，能算"多少**人**来过" |
| 日期按环境时区算 → 跨天错位 | 固定按 UTC+8 业务时区 |
| 线上报错看不到 | 未捕获异常自动记入事件流 |

### 已知局限（还没解决，用的时候要记得）

- **没有留存**：现在看不出「第 2 天还来吗」
- **没有对照组**：改了东西只能说「指标变了」，**不能**说「因为这个变的」（要 A/B 才能归因）
- **样本自选择偏差**：来的都是看了宣传文章的人，不代表知乎普通用户
- **`copy_comment` 有漏**：用户可能手动选中复制（不走按钮）

---

## 五、怎么用（操作指引）

1. **每周看一次就够**（数据量小的时候，天天看只会被噪声带偏）
2. 顺序：**先看北极星趋势 → 再看漏斗哪一步掉 → 最后看定性信号找原因**
3. 从数据到行动，走完闭环：
   ```
   发现异常 → 提出假设 → 改一个变量 → 过几天再看同一指标 → 有提升才算改对
   ```
   （这跟 `prompt-regression-loop` 是同一个思路，只是那边用离线样本、这边用真实用户）

---

## 六、术语小抄（写材料/面试会用）

| 词 | 一句话 |
|---|---|
| 北极星指标 | 最能代表产品价值的**那一个**数字 |
| 漏斗 | 用户从进入到完成的每一步，用来看流失 |
| 转化率 | 走到下一步的人 / 上一步的人 |
| 留存 | 用过的人过几天还回不回来（我们还没做）|
| 效度 | 测的是不是你想测的东西 |
| 信度 | 测的结果稳不稳、可不可重复 |
| 归因 | 指标变化**到底**是不是你这次改动引起的 |
