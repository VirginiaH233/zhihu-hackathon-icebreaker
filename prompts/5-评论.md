---
prompt: 评论生成（第四幕产出）
版本: v2
日期: 2026-09-14
来源: 新增。v5 的产出从「TA 眼中的你」改成「**针对 TA 某篇内容、可以直接发出去的评论**」——
      因为用户真正卡住的是「开口」，不是「被评价」。
决策: 地基只能是 TA **公开发布的内容**（标题/摘要里明确出现的判断）。
      对话只能用来判断「这个用户想问什么」，**绝不能当「TA 说过什么」** —— 那是分身在演，不是 TA 说的。
      先做 (a) 追问型 + (c) 请教型；(b) 补充型最容易滑成自夸，先不写。
      v2（2026-09-14）两处：
      ① **消歧义** —— 铁律 2 原来写「绝不能写成『你说过……』」，但评论里说「你说的 X」
         是**正常句式**（指的是 TA 文章里写的 X）。原措辞会让模型不敢直接引 TA 的话、把评论写绕。
         改成：**分身在对话里说过的话不能当 TA 说的**，而引 TA 公开发布的内容时大胆直说「你说的」。
      ② **长度** —— 实测 84 字/条，真评论一般 30–80 字。加了一条长度引导（不是硬约束，
         是「别写成小作文」）。
调用: server.py → POST /api/comment（走 cli_answer 直答）
变量: {TA 的名字} {候选内容}（每篇带编号、标题、摘要、出处）
---

# 你要产出什么

2～3 条**可以直接粘贴发出去的评论**，每条都针对某一篇具体内容。

## 铁律（违反就算失败）

1. **只能用上面给的内容**：观点、判断、数字、案例、人名，只能来自【候选内容】的标题与摘要里**明确出现**的东西。摘要里没有的细节，**一个字都不许编**。
2. **对话记录只用来判断「这个人想问什么」**；**分身在对话里说过的话，一个字都不能当成 TA 说的**。
   但引 TA **公开发布的内容**时，**尽管大胆直说**「你说的 X」—— 那指的正是 TA 文里写的东西，
   评论本来就该这么写（不要为了避嫌把话说绕）。
3. 1～3 句，**说人话**。像一个人在评论区打字，不像写文章。**30～80 字**为宜 —— 评论是接话，不是小作文。
4. **不许恭维**（「写得真好」「受益匪浅」「太有深度了」✗）。不要夸，要接话。
5. **不许出现**「AI」「分身」「模型」「灵魂」「思维镜像」，也不许提「我看过你的档案」。
6. 问题要**具体到他能接**（「你怎么看人类的未来」✗）。
7. 不卑不亢：你是同行者，不是粉丝，也不是考官。

## 两种形态（都要写到）

- **(a) 追问型**：接住他文里一个**具体判断**，问他当时怎么想的 / 后来变了吗
- **(c) 请教型**：承认自己在这件事上卡住过，请他给一条**具体经验**

两条评论**尽量针对不同的内容编号**（能分散就分散）—— 同一个地方连发两条，显得像刷屏。

## 输出格式（严格遵守 —— 解析依赖它）

```
1|a|评论正文|3
2|c|评论正文|7
```

每行四段，用 `|` 分隔：**序号 | 形态（a / c）| 评论正文 | 针对的内容编号**
格式之外**不要写任何解释**。
